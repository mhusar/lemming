#set($indent = "  ")
${indent}<sublist key="$sublist.getKey()">
#foreach ($item in $sublist.getItems())
#set($following = $item.getFollowing())
#set($keyword = $item.getKeyword())
#set($lemma = $item.getLemma())
#set($location = $item.getLocation())
#set($pos = $item.getPos())
#set($preceding = $item.getPreceding())
#set($type = $item.getType())
#set($punctuation = $item.getPunctuation())
#set($indent = "    ")
#if( "$!punctuation" == "" )
${indent}<item type="$type" location="$location" preceding="$preceding" following="$following" lemma="$!lemma" pos="$!pos">$keyword</item>
#else
${indent}<item type="$type" location="$location" preceding="$preceding" following="$following" lemma="$!lemma" pos="$!pos">$keyword<punctuation>$punctuation</punctuation></item>
#end
#end
#set($indent = "  ")
${indent}</sublist>
